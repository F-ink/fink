{% extends 'base.html.twig' %}
{% block stylesheets %}
	<link href="{{ asset('styles/admin.css') }}" type="text/css" rel="stylesheet">
{% endblock %}
{% block body %}
<nav class="navbar navbar-expand-lg navbar-light navAdmin">
		<div>
			<a href="{{ path('admin_') }}"><img class="logoAdmin" src={{ asset ('img/logo.png') }} alt="Le logo Fink"/></a>
		</div>
		<div class="container-fluid">
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNav">
				<ul class="navbar-nav">
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{ path('admin_') }}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('artist_add') }}">Artistes</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('admin_styles_home_styles') }}">Styles</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('home') }}">Accès au site</a>
					</li>
				</ul>
			</div>
		</div>
	</nav>

{% endblock %}